{
  "name": "install-system",
  "state": "installed-system",
  "parentState": "installer-ssh",
  "stateMaterial": "live-install:nixos:immutability={{immutabilityVersion}}:implementation={{immutabilityImplementation}}:mode={{immutabilityMode}}:boot={{bootloader}}:layout={{diskLayout}}:swap={{swapSize}}:desktop={{desktop}}:ram={{memory}}:cpus={{cpus}}:source={{sourceHash}}",
  "args": {
    "immutabilityVersion": "v2",
    "immutabilityImplementation": "immutability",
    "immutabilityMode": "reset",
    "immutabilityEnabled": "true",
    "bootloader": "systemd-boot",
    "diskLayout": "btrfs-subvolumes",
    "swapSize": "2G",
    "desktop": "none"
  },
  "workDir": ".vm/e2e/{{state}}-{{stateHashShort}}",
  "diskSize": "30G",
  "memory": "8G",
  "cpus": 4,
  "sshPort": 2224,
  "operations": [
    { "op": "require-state" },
    { "op": "reset-state" },
    { "op": "inherit-state" },
    { "op": "validate-immutability-v2" },
    { "op": "restore", "name": "live-ssh" },
    { "op": "boot-iso" },
    { "op": "wait-ssh", "timeout": 180 },
    { "op": "copy-workspace", "local": ".", "remote": "/etc/nixos" },
    { "op": "configure-vm-host" },
    { "op": "install-nixos" },
    { "op": "snapshot", "name": "installed" },
    { "op": "record-state" },
    { "op": "stop" }
  ]
}
