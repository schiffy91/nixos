import std.*;
import ../../btrc/core/*;
import ./spec/*;
import ./qemu/*;
import ./graph/*;
import ./spec.btrc;
import ./qemu_harness.btrc;
import ./runner.btrc;

/* VM test graph module aggregator.
 *
 * std.graph owns the JSON graph model, parser, CLI arg helpers, and dependency
 * traversal. The local graph runner only maps graph nodes onto VM specs and
 * QEMU-backed operations.
 */
